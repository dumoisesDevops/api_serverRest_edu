name: End-to-end tests
on: push
jobs:
  cypress-run:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      # Instalar dependências e cache
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Cache npm dependencies
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-modules-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-modules-

      - name: Install dependencies
        run: npm install

      # Executar Cypress sem relatórios personalizados, apenas com status
      - name: Run Cypress tests
        run: npx cypress run --env allure=false

      # Opcional: Verificar se os testes passaram
      - name: Check test result
        run: |
          if grep -q "All tests passed!" cypress/results/*; then
            echo "All tests passed!"
          else
            echo "Some tests failed"
            exit 1
          fi
